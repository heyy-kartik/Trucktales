// This file contains Convex database queries and mutations
// Run `npx convex dev` to generate the proper _generated files
// These are stub implementations for build purposes

import { v } from "convex/values";

// Query all incidents
export const getIncidents = async () => {
  return [];
};

// Query all trucks
export const getTrucks = async () => {
  return [];
};

// Create new incident
export const createIncident = async () => {
  return "";
};

// Update truck location
export const updateTruckLocation = async () => {
  return "";
};

// Get nearby trucks for a given location
export const getNearbyTrucks = async () => {
  return [];
};
